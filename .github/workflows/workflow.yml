name: CI

on: [push]

jobs:
  show-xcode-10-15:
    runs-on: macOS-10.15
    steps:
      - name: Show Xcodes
        run: ls /Applications | fgrep Xcode
  show-xcode-10-14:
    runs-on: macOS-10.14
    steps:
      - name: Show Xcodes
        run: ls /Applications | fgrep Xcode
  show-xcode-10-13:
    runs-on: macOS-10.13
    steps:
      - name: Show Xcodes
        run: ls /Applications | fgrep Xcode
  show-xcode-10-12:
    runs-on: macOS-10.12
    steps:
      - name: Show Xcodes
        run: ls /Applications | fgrep Xcode
